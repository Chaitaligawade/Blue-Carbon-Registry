# Blue Carbon Registry

Project uploaded from the provided Blue Carbon Registry project archive.

The complete cleaned project is included in `blue-carbon-project.zip`.

Excluded from the archive: `node_modules`, `.vite`, `.env`, and the original nested `.git` directory.
